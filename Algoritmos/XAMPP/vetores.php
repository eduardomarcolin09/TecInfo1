<?php
    // Vetores - PHP

    $eduardo = ['abacaxi', 3, 5.5, true];

    // -> Contar itens do vetor (array)
    # echo count($eduardo);

    // -> Verificar valor real de uma variável
    # var_dump($eduardo[3]);

    // -> Verificar se uma variável (ou índice) existe
    # isset($abacaxi);
    # isset($eduardo);
    # isset($eduardo[0])