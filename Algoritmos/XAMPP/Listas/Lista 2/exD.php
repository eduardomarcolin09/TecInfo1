<?php
    // d) Faça um algoritmo que receba a altura e a largura de um retângulo e, em
    // seguida, escreva a área.
    
    $area = $_GET['altura'] * $_GET['largura'];
    echo('A área do retângulo é ' . $area);