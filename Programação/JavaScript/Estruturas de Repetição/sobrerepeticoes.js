/*    Estruturas de repetição

    < -- WHILE -- >
    
    Essa estrutura tem zero ou mais interações;
    Precisa criar uma variável para controlar as interações;
    As interações terminam quando a condição se torna falsa;
    Aas interações também póde terminar com o comando break;

    =-=-= Sintaxe =-=-=

    while(condição) {
        // A condição DEVE ser uma expressão lógica.
        comandos
    }

    < -- FOR -- >

    Possui um bloco de inicialização (executado apenas uma vez);
    Os comandos são executados enquanto a condição seja verdadeira;
    O bloco de incremento é executado smepre depois dos comandos;

    =-=-= Sintaxe =-=-=

    for(inicialização; condição; incremento){
        comandos
    }

*/
